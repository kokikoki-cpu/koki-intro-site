# I am KOKI サイト設計メモ

このファイルは今後の実装・別セッションでの作業時に参照するための決定事項まとめ。
デザインや構成を変える際は、まずここを読んでから作業する。

## 【重要】2026-08-14: Next.jsへ全面移行済み（この節を最初に読む）

このリポジトリ（`koki-intro-site`、`/Users/k.s/Claude/koki-site`）は、以前は静的
HTML/CSS/JSサイトだったが、**Next.js 16（App Router / TypeScript / Tailwind v4）に
全面的に作り直した**。以下より前の節（カラーパレットやカードの見た目のルールなど）は
「デザイン方針としては今も有効」だが、**具体的な実装（`.hero__inner`のようなクラス名や
`css/`配下のファイルなど）はもう存在しない**。コードを触る前に実際のファイル構成
（`src/app/`, `src/components/`, `src/lib/data.ts`）を確認すること。

- ページ構成: `src/app/page.tsx`（トップ）, `src/app/world/page.tsx`,
  `src/app/people/page.tsx`, `src/app/sports/page.tsx`
- 共通レイアウト: `src/app/layout.tsx`（Header/Footer/フォント読み込み）
- コンテンツデータ: `src/lib/data.ts`（旧`js/data.js`のTS移植版。これが唯一の情報源）
- 共通UI: `src/components/Header.tsx`, `Footer.tsx`, `Modal.tsx`
  （国/人物/スポーツの詳細ポップアップはすべてこの`Modal`を共用）
- デザイントークン（色・フォント変数）とアンビエント背景のCSSキーフレームは
  `src/app/globals.css` に集約（`.page-hero-photo` / `.globe-layer` / `.walker` /
  `.pulse-dot` など）
- **トップページだけの目玉機能**: `src/components/ShootingGameGate.tsx`。
  「詐欺師撃退シューティングゲーム」をクリア（または合言葉）しないとサイト本編が
  見られないゲート。クリアするとモンゴル旅行動画（`public/videos/mongolia.mp4`）が
  再生されてからサイトへ進む。敵キャラの丸アイコンは本人の顔写真を丸くクロップした
  `public/images/game/scammer.jpg`

### デプロイ方法（重要）

- GitHubの`main`にpushしても、Vercel側の自動ビルドが一度うまく反映されない事故が
  あった（Framework Presetは正しく`Next.js`なのに本番が404になった）。原因ははっきり
  特定できていないが、**Vercel CLIから直接`vercel --prod`を実行することで確実に直る**
  ことを確認済み。今後の本番反映はこれを使う:
  ```bash
  cd /Users/k.s/Claude/koki-site
  npx vercel --prod
  ```
  （このマシンはCLIログイン済み・`.vercel/project.json`で`koki-intro-site`に
  リンク済みなので、そのまま実行できる。`Not authorized`エラーが出たら
  `.env.local`を削除してから再実行すると直ることがある＝キャッシュされた
  `VERCEL_OIDC_TOKEN`が古くなっている可能性）
- `vercel link`を素で実行すると、ディレクトリ名（`koki-site`）から**新しいプロジェクトを
  勝手に作ってしまう**ので注意。必ず `vercel link --project koki-intro-site --yes`
  のように既存プロジェクト名を明示すること（一度この事故で`koki-site`という空の
  プロジェクトが誤って作成されており、Vercelダッシュボードに残っている）
- Node.js/npmはこのマシンに元々入っておらず、`nvm`経由でインストール済み
  （`~/.zshrc`に`NVM_DIR`の初期化が追記されている）

## 2026-08-17: シューティングゲートをThree.js化（3D・明るい色）

「誰も見たことのない自己紹介サイト」の方向性をさらに推し進め、トップページの
シューティングゲート（`src/components/ShootingGameGate.tsx`）を**Canvas 2D描画から
Three.js（WebGL）の3Dシーンに全面刷新**した。狙いは「立体感は欲しいが、既存の
グラスモーフィズム/ダークネイビー系リファレンス画像のような『AIっぽいテンプレ感』は
0にしたい、明るく楽しい色にしたい」という本人の要望。

- 舞台は「夕焼けの砂漠ラリー」: キャンバスグラデーションの夕焼け空
  （オレンジ→ゴールド）、低ポリの砂丘地形、レーン脇を流れる緑/クレイ色の
  コーンマーカーで疾走感を演出。ダークネイビー・グラスモーフィズムは不採用。
- プレイヤーはロケット状の低ポリメッシュ（グリーン+クレイのフィン）、敵は本人の顔写真
  （`scammer.jpg`）をテクスチャ貼りした球体、弾は発光イエローのカプセル。
  すべて「アニメ風アウトライン」（法線反転メッシュを少し拡大してBackSideで重ねる手法）
  付きで、フラットシェーディング中心のトゥーン調。
- **ゲームのロジック（移動・自動発射・スポーン間隔・当たり判定・スコア/残機・勝敗
  判定のしきい値）は一切変更していない** — 前回チューニング済みの数値をそのまま
  流用し、変更したのは描画レイヤーのみ。よって難易度感は据え置き。
- 実装上の注意: このコンポーネントは`checked`（sessionStorageの解錠済みチェック）が
  `true`になるまで`return null`するため、Three.jsのセットアップ用`useEffect`は
  依存配列を`[]`ではなく`[checked]`にする必要がある（`[]`だと初回の`null`レンダー時に
  `mountRef.current`がまだ存在せず、レンダラーが二度と作られない）。
- 動作確認はブラウザツールの自動化環境だと`document.visibilityState`が`hidden`に
  なりがちで`requestAnimationFrame`が実質止まる（体感フレームが進まない）。これは
  テスト環境固有の制約であり、実際にタブがフォーカスされているユーザー環境では
  通常通り60fpsで動く。

## 2026-08-17: 全セクションをゲームで解錠する構成に（Three.js全面採用）

> **2026-08-20 追記**: 「全面採用」は現状と違う。実際に `three` を import しているのは
> `ShootingGameGate.tsx` / `games/FlightGame.tsx` / `games/CareerRunGame.tsx` /
> `games/three-kit.ts` の4つだけ。`MemoryGame.tsx` と `SprintGame.tsx` は
> three-kit から型（`GamePhase`）だけを借りた **DOM+CSS実装**、トップの星系
> （`CosmosHome.tsx`）も **CSSアニメーション**で、three.js は使っていない。
> つまり5ゲーム中3つがThree.js。

「誰も見たことのない自己紹介サイト」を突き詰め、**トップのゲートだけでなく各ページの中身も
ゲームをクリアしないと読めない**構成にした。あわせてトップページのヒーローを3D化。

### 共通の仕組み

- `src/lib/unlock.ts` — 解錠状態を sessionStorage で管理する唯一の窓口。タブを閉じると
  リセットされる（次の来訪者はまた最初から遊べる）ので localStorage は使わない。
  - `useIsUnlocked(id)` / `useUnlockedFrom(ids)` は **`useSyncExternalStore`** で購読する。
    sessionStorage は React の外の状態なので、`useEffect` + `setState` で読むと
    lint（`react-hooks/set-state-in-effect`）に弾かれるうえ SSR と食い違う。この形なら
    サーバー描画時は「未解錠」、クライアントで即座に実状態へ切り替わる。
  - 合言葉「こうきいつもありがとう」を入れると `koki-unlock-all` が立ち、全部まとめて解錠。
    ゲートの解錠キーも `gate` として同じ仕組みに統合済み（旧 `koki-gate-unlocked` は廃止）。
- `src/components/games/three-kit.ts` — 全ゲーム共通の3Dパーツ。`createStage()`（レンダラー生成・
  リサイズ追従・破棄まで面倒を見る）、`skyTexture()`、`lowPolyGround()`、`outlineFor()`（法線反転
  メッシュによるアニメ調の輪郭線）、`toonMat()`、`addLights()`。色は globals.css のトークンと同じ値。
- `src/components/games/GameShell.tsx` — ゲーム共通のオーバーレイUI（タイトル / 遊び方 / HUD /
  キャンバス枠 / 失敗・クリア画面 / 合言葉スキップ）。各ゲームは中身のロジックだけ書けばよい。

### 各セクションのゲーム

| 場所 | ファイル | 中身 | クリア条件 |
|---|---|---|---|
| トップのゲート | `ShootingGameGate.tsx` | 夕焼けの砂漠でエチオピア詐欺師を撃退 | 10体撃破（残機5） |
| 世界地図の各国 | `games/FlightGame.tsx` | その国へ飛ぶ。輪をくぐる | 5回通過（3回外すと失敗） |
| 人図鑑の各人物 | ~~`games/EncounterGame.tsx`~~ → **`games/MemoryGame.tsx`** | 光った順を記憶して押し返す | 下記2026-08-20の表を参照 |
| スポーツの各競技 | ~~`games/RallyGame.tsx`~~ → **`games/SprintGame.tsx`** | 連打で走る＋ハードルを跳ぶ | 下記2026-08-20の表を参照 |
| トップの職歴 | `games/CareerRunGame.tsx` | 壁のすき間を抜けて関門を突破 | 6関門（**残機なし・かすったら即終了**） |

- **職歴が最難関**。本人の最も個人的な情報なので、サイトで一番むずかしいゲームにしてある。
  未解錠のときは職歴の各行が「？」で伏せられる（`CareerCard.tsx`）。関門をひとつ越えるごとに
  そのキャリアの1行がキャンバス上に表示される＝**自分の経歴を実際に走って辿る**という作り。
  関門を越えるたびに速度が上がり、壁のすき間が狭くなる（下限 `MIN_GAP` で運ゲー化を防止）。

- 人図鑑は**ポケモン図鑑方式**にした。未発見はシルエット＋「？」で名前も出現場所も伏せ、
  ゲームに勝つと正体が判明する。ゲーム中のタイトルもわざと「？？？」のままにしている。
- 世界地図は未到達のピンをクレイ色で点滅、到達済みはアクセントのグリーンに変える。
  地図のピンは16pxと小さいので、**見た目を変えずに44px相当の当たり判定を重ねてある**（スマホ対策）。
- スポーツは未突破のタイルをグレースケールにして、突破すると色が戻る。

### トップページは一枚の宇宙（`CosmosHome.tsx`）

当初は「明るいヒーロー（3D写真リング）＋暗い太陽系」の2段構成だったが、
**上下でニュアンスが分かれる**という指摘を受けて、トップページ全体を一枚の宇宙に統一した。

- 3Dの写真リング（`HeroStage3D.tsx`）は廃止。旅の写真は**流れ星**として夜空を横切る形にした。
  - `.shooter` を進行方向へ `rotate` しておき、中身をローカルの +x 方向へ流す。こうすると
    尾（`.shooter__trail`）の向きと進行方向が必ず一致する。写真だけは
    `rotate(calc(var(--angle) * -1))` で正立させる。
  - 8本を別々の周期・遅延で流し、常時2〜3本だけ見えるようにしている。
- 見出し（名前・タグライン・プロフィール）も同じ宇宙の中に置き、白文字で星空に重ねる。
- **ヘッダーとフッターも暗くした**（サイト全体）。ここが明るいままだと宇宙の上端に
  はっきりした境界線ができて、統一した意味がなくなる。他の3ページは写真ヒーローに
  暗いスクリムが乗っているので、暗いヘッダーの方がむしろ馴染む。
- トップは three.js を使わなくなった（ゲート・職歴ゲームのみ、どちらも動的読み込み）。

太陽は本人の顔写真、5つの惑星（強み3つ・職歴・趣味）がその周りを公転する。

- 惑星の中身: 行動力 / 好奇心 / 体力 → 各ページへのリンク。職歴 / 趣味・活動 → その場でパネルを開く。
  職歴が未解錠なら惑星に「？」が付き、押すと `CareerRunGame` が始まる。
- 公転の作り: `.orbit`（正方形＋`border-radius:50%`）を回し、その円周上に置いた惑星を
  **同じ長さ・同じ遅延の逆再生**（`animation-direction: reverse`）で打ち消して常に正立させる。
  回転するのは円なので、この要素の `border` がそのまま軌道線を兼ねる。
- `delay` は `dur × (index / 5)` で決める。全部 0 にすると5つとも真上から出発して団子になる。
- **`.system:has(.planet:hover)` で系全体を一時停止**する。動いている的は押せないので必須。
  キーボード操作用に `:focus-within` でも止める。
- **`.orbit` には `pointer-events: none` が必須**（惑星側で `auto` に戻す）。`.orbit` は
  `border-radius: 50%` の正方形なので、見た目は「線」でも当たり判定は**円形の面**を持つ。
  外側の軌道は内側の惑星より後に描画されるため、これを外すと**外側の軌道が内側の惑星の
  クリックを丸ごと奪う**（実際に、一番上の「趣味」以外が押せない不具合になった）。
- 軌道の直径と太陽・惑星のサイズは相互に制約がある:
  `最内周の半径 − 惑星の半径 > 太陽の半径`。これを満たさないと最内周の惑星が太陽に食い込む
  （スマホでこれを踏んだので、太陽を `clamp(78px,21vw,148px)` に、最内周を 46% に調整した）。
- 星はモジュール読み込み時に**種を固定した乱数**で位置を決める。描画ごとに乱数を振ると
  サーバーとクライアントで食い違ってハイドレーションエラーになる。
- 注意: `globals.css` の `a { color: inherit }` は**レイヤー外**なので、Tailwind のユーティリティ
  （`@layer utilities` 内）より強い。暗い背景の上のリンクは、セクション側で文字色を継承させること
  （惑星のラベルが見えない不具合をこれで踏んだ）。
- 注意: flex の子に `mx-auto` を付けると**交差軸の stretch が無効になる**。`max-w-*` と併用して
  いる要素は `w-full` も付けないと、最大幅まで広がらず最も広い子の幅に縮む
  （セクションを flex にした途端、見出しが中央寄りに見える不具合をこれで踏んだ）。

### 難易度の決め方（`tools/sim-gate.mjs`）

「初見の人が50%くらいで勝てる」を勘で当てるのは無理なので、**ゲートのゲームロジックを
そのまま再現したシミュレータ**を用意した。初心者モデル（反応が鈍い・先読みしない・狙う敵を
間違える・時々手が止まる）を何百回も走らせて勝率を測り、そこから定数を決めている。

```bash
cd /Users/k.s/Claude/koki-site
node -e "import('./tools/sim-gate.mjs').then(m => console.log((m.winRate({...m.CURRENT, juke:true, startLives:3, fireInterval:240, spawnInterval:780, speedMin:2.8, speedSpread:1.2, driftMin:80, driftSpread:45, dodgeStart:210, hitTolX:3, hitTolY:7}, m.BEGINNER, 1500)*100).toFixed(1)+'%'))"
```

2026-08-17時点の実装値で **初見 55% / 慣れた人 76%**（調整前は初見でも100%だった）。

分かったこと:

- **敵の落下速度が一番効く**。遅いと、弾幕の壁を通過する間に必ず当たってしまい、
  残機を1にしても当たり判定を厳しくしても勝率が下がらない（実測で92%以上のままだった）。
- 次に効くのが**発射間隔**。狭いと弾が壁になり、横位置を合わせるだけで倒せてしまう。
- **終盤の切り返し（juke）**を入れて、単純な追従では当たらないようにした。
  `Math.sin(progress * Math.PI * 1.5)` で「よける → 逆に切り返す」を1回やる。
- 当たり判定は弾4px・敵26pxの実寸に近い値まで詰めた（以前は横23pxまで当たる大盤振る舞いだった）。
- 調整は「初見50%」だけを見ず、**慣れた人との差**も見る。差が10pt程度しかない設定は
  運ゲーで、腕が上がっても報われない。上記は+21ptあるので、練習が効く。

※このモデルは実際の人間の近似なので、本人の体感とはズレる。ズレたら上のコマンドで
狙いの勝率を出し直してから定数を動かすこと（勘で1つだけ緩めると一気に簡単になる）。

### 検証時の注意（このプレビュー環境の癖）

- ブラウザのプレビューペインは、**アニメーション中の要素の画像を合成しきれずに空で写る**ことがある。
  写真が出ていないように見えても、まず `getAnimations()` で一時停止してから撮り直して確認する。
  DOM 上で画像が読み込み済み（`naturalWidth > 0`）かつ可視なら、実装ではなく撮影側の問題。
- 同じ理由で `document.visibilityState` が `hidden` になりがちで `requestAnimationFrame` が
  ほぼ止まる。ゲームの動作確認は、フレームを進めるために何度もスクリーンショットを撮るか、
  一時的な検証用フックを仕込んで判定だけを直接叩く。
- **クリックの確認に `element.click()` を使ってはいけない**。JSの `.click()` は当たり判定を
  飛ばして直接発火するので、「他の要素に覆われて押せない」類の不具合を検出できない
  （太陽系の惑星が押せない不具合を、これで一度見逃した）。
  当たり判定は `document.elementFromPoint(x, y)` で「最前面がその要素か」を確かめ、
  さらに実際のマウスクリックで裏付ける。
- ブラウザツールの `ref` 指定クリックは**祖先の CSS `transform`（回転など）を考慮しない**ため、
  公転中の惑星では回転前の位置を押してしまう。`getBoundingClientRect()` で実際の座標を測り、
  スクリーンショットの縮尺（撮影画像の幅 ÷ `innerWidth`）を掛けて座標指定でクリックする。

### ゲートクリア後の場面転換（`StampedeTransition.tsx`）

モンゴル動画からサイト本編へ、いきなり切り替わると味気ないので**映画的な場面転換**を挟む。
動画に映っている牛がそのまま画面に雪崩れ込んでくる、という見立て。約3.4秒。

1. シネスコの黒帯が上下から閉じる
2. 牛のシルエットが手前・奥に分かれて画面を横切る（画面が揺れる）
3. 巻き上がった砂ぼこりが画面を覆い、ワイプの役目を果たす
4. 砂の中にタイトル「清水航樹 / 旅。野生。世界。」が浮かんで消える
5. 全体がフェードアウトしてサイトが現れる

- 牛はSVGのシルエット。**肩のこぶ・大きな2本の角・長い脚**を入れないと豚に見えてしまうので、
  この3点は崩さないこと（最初の版がまさに豚に見えて作り直した）。
- 動きはすべて `globals.css` の `@keyframes`（`stampede-*`）。JS側は総尺のタイマーだけ持つ。
  `prefers-reduced-motion` では全アニメーションを1msに潰して即座に本編へ送る。
- 転換に入るとき動画を `pause()` する（暗転の裏で音だけ鳴り続けるのを防ぐ）。
- **転換中は動画画面を描画しない**（`if (transitioning) return <StampedeTransition/>`）。
  裏に残しておくと、砂ぼこりが晴れる瞬間にサイトではなく黒い動画画面が一瞬見えて
  「挙動がおかしい」感じになる。フェードアウトはサイト本体へ直接繋げること。
- タイトルは最後まで `opacity: 1` を保ち、消えるのは全体のフェードアウトに任せる。
  ここで個別に消すと二重にフェードして瞬く。

### トップページのヒーロー（`HeroStage3D.tsx`）

- 旅の実写を3D空間の円周に並べ、ゆっくり自動回転＋**ドラッグで手回しできる**（離すと惰性で回る）。
  マウス位置にカメラがわずかに追従して立体感を強調する。
- 写真は「太い枠線のカード」というサイトの共通言語に合わせ、一回り大きい黒い板を裏に置いて額装に。
  1枚ごとに高さと傾きを変えて、雑誌的な非対称さを出している。
- 背景色・フォグはページ背景（`--color-bg`）と同色にして、キャンバスの縁が紙に溶けるようにした。
- スマホ（縦長）ではカメラを寄せて画角を広げる（`fitCamera()`）。これをやらないと写真が
  小さく写って画面上半分が空っぽになる。ヒーローの高さもスマホでは 46vh に抑えている。
- 旧 `EgyptParallax.tsx`（マウス視差のピラミッド背景）はこれに置き換わったので削除済み。

### 注意点（ハマったところ）

- **`/_next/image` は `w` と `q` を検証する**。`w` は next.config の deviceSizes / imageSizes に、
  `q` は `images.qualities`（既定は `[75]` のみ）に含まれる値しか通らず、外れると 400 が返って
  テクスチャが真っ白になる。`three-kit.ts` の `optimizedSrc()` で w=640 / q=75 に固定してある。
- Three.js の **`MeshToonMaterial` は `flatShading` を持たない**。低ポリの面を立たせたい時は
  `MeshPhongMaterial({ flatShading: true })` を使う（`toonMat()` がそれ）。
- ゲームを開くコンポーネントは `next/dynamic` の `ssr: false` で読み込んでいる。three.js は
  重いので、ゲームを開くまでダウンロードさせない。

## コーディングルール（厳守）

**`padding` / `margin` はショートハンドで書かず、変更したい辺だけを個別プロパティ
（`padding-top` / `margin-left` など）で指定する。** これは今後このサイトで**必ず統一する**
ルール（特別な理由がない限り例外を作らない）。

理由: `.hero__inner`に「上だけ余白を変えたい」つもりで`padding: clamp(16px,3vw,24px) 0 0;`
と書いたところ、指定していない右・左・下も`0`で上書きされ、既存の`.wrap`の左右余白
（`var(--gutter)`）が消えて、Hero写真だけ左右フルブリードになり、下のコンテンツと
左端が揃わなくなる不具合が起きた（詳細は下記「Hero写真とキャプションだけ左右の余白が
消えていた事故」を参照）。ショートハンドは書いていない辺も含めて4辺すべてを上書きする、
という仕様が原因。

適用範囲: このプロジェクトのCSS全般（すでにあるルールを1辺だけ調整する場合も含む）。
4辺すべてを意図的に一括指定する場合（新規要素で最初から4辺決め打ちするときなど）は
ショートハンドのままでよい。「一部の辺だけ触る」ときに限って個別プロパティを使うこと。

## 公開状況

- GitHub: https://github.com/kokikoki-cpu/koki-intro-site （`main`ブランチにpush済み）
- 本番URL（公開用）: **https://koki-intro-site.vercel.app/** ← これを共有する
  - 注意: Vercelのデプロイごとの個別URL（`koki-intro-site-xxxxxxxx-kokikoki-cpus-projects.vercel.app`
    のようにハッシュが付くもの）はVercelのSSO認証がかかっていて、ログインしていない人は見られない。
    共有・応募には必ず上のハッシュなしの本番URLを使うこと
- pushはHTTPS＋Personal Access Token（`repo`スコープ）で認証。トークンはmacOSのキーチェーン
  （osxkeychain credential helper）に保存済みなので、次回以降 `git push` するだけで通る

## サイト概要

清水航樹さんの自己紹介サイト。会社のルールで「HTML/CSS/JS、CMS禁止、3ページ以上、
JSアニメーション1箇所以上、PC/SPレスポンシブ」という制約がある。静的サイトとして
GitHub管理 → Vercelデプロイを予定。Supabaseはアクセス数/いいねなどの軽量機能用に
使う予定（まだ未実装）。

## ページ構成（4ページ）

ブランド表記は **「I am KOKI」**（"I'm KOKI" ではない）。ロゴ・nav・titleタグすべてこれで統一。

- `index.html` — I am KOKI（トップ）: 基本情報＋強み3つ。強みをクリックすると対応ページへ
  - 行動力 → world.html / 好奇心 → people.html / 体力 → sports.html
- `world.html` — あぁ素晴らしき地球: 世界地図に訪問国のピン、クリックでエピソードポップアップ
- `people.html` — **世界のクセ強人類**（旧名「素晴らしき人類」から変更済み）: 人図鑑（横スライドカード）。
  No.1は清水さん本人でキャリア年表＋今後の目標も表示。No.2以降の名前は「忘却」のような抽象語ではなく、
  「宿のオーナー」「インドのおばちゃん」「アフリカの詐欺師」のように**具体的な呼び名**にする
- `sports.html` — 身体こそすべて: 好きなスポーツのグリッド、クリックで詳細ポップアップ

## デザイン方針（重要・厳守）

**清水さん個人のWebデザインの好みは `~/.claude/skills/koki-design-taste/SKILL.md` に
言語化されている（このサイト専用ではなく、彼が作る/直すどのサイトにも適用する好みの根っこ）。
デザインの相談や見た目の変更をする前に必ずこれを読む。** 見た目のレビュー・チェックには
同スキルの `references/checklist.md`（28項目・OK/△/―判定）を使う。2026-08-13に
`サイトデザインチェックリスト.xlsx`としてこのチェックリストで実際にレビューし、
△だった7項目（アクセント色が2色ある/サブページに写真ヒーローがない/カードが角丸+
ソフトシャドウ依存/カードが均等な箱の羅列/縦スクロールのストーリー性が弱い/説明文が残っている）
のうち6項目を修正済み（「縦スクロールのストーリーテリング」は大掛かりな追加コンテンツが
必要なため保留、要相談）。

過去に一度「AIっぽい」と指摘され、修正した経緯がある。以下のパターンに戻さないこと。

### 自分（AI）が無自覚にやりがちな癖 ← 毎回ここを読んで自己点検する

「AIっぽい」と言われた原因を並べておく。手が滑るとここに戻るので、作る前と出す前に読む。

1. **光らせて逃げる**。`box-shadow` の外側グロー、`blur`、`drop-shadow` を足して「豪華」に
   見せようとする。実際には量産のSFイラストになる。光は原則ひとつだけ、または無し。
2. **紫〜ピンクの宇宙グラデーション**。radial-gradient を何枚も重ねた星雲。これは本人の
   NGパターン「意味のないグラデーションメッシュ」そのもの。色は増やさない。
3. **キャンディ色**。#d86fa8 のような彩度の高いピンク/紫/シアン。パレットはアース系のまま、
   夜空でも土や砂の色域から外れないこと。
4. **丸くしてふわっとさせる**。角丸＋ソフトシャドウ＋淡いグラデ。枠線と余白と写真の比率で
   強弱をつけるのが本人の好み。
5. **全部を同時に動かす**。脈動・回転・明滅を一画面に何種類も置く。動きは「意味のある1つ」に絞る。
6. **参考画像をそのまま真似る**。渡された画像がAI生成イラストのことがある（銀河の絵、
   スポーツ惑星の絵はどちらもAI生成だった）。**見た目を写すのではなく、そこにある狙い
   （宇宙の広がり／集める楽しさ）だけを抜き出して、本人のパレットと実写で組み直す**。
7. **実写を装飾に格下げする**。本人のサイトは実写が主役。写真を背景の小片にして、
   代わりにCSSで描いた飾りを主役にしたら、それは間違い。

判断に迷ったら「これは彼の旅の写真が主役か？ 色は3〜4色に収まっているか？ 光っているものは
1つ以下か？」を確認する。

### NGパターン（やってはいけないこと）
- 見出しに装飾的な英語ラベルを付けない（例: "TRAVEL" "PEOPLE" "SPORTS" "SELF INTRODUCTION"）
  → 日本語の見出しのみで良い
- カードや項目に "01" "02" "03" や "No.1" "No.2" のような連番ラベルを付けない
- 補足の注意書き・キャプション的な一文を極力入れない（説明過多にしない）
- **操作方法を文章で置かない**。「惑星を押すと中身が見られる」「ピンを押すとフライトが始まる」
  のような案内はすべて削除済み。押せることは見た目で伝える（惑星は静かに脈打つ光の輪
  `planet-breathe` ＋ `cursor: pointer`、未解錠は「？」）。画面に残す文字は進捗の数字だけ
  （到達 3/9、発見 2/4 など）
- **ゲームを簡単にしない**。「つまらないから」と明言されている。難易度を触るときは
  下記のシミュレータで勝率を確認してから動かす
- 暖色オレンジ×クリーム×角丸カード×ソフトシャドウの「AI生成っぽい」配色・質感の組み合わせ
- カラーパレットに黄色（マスタード等）を使わない

### 良いパターン
- 本文・見出しなど通常のテキストは**左揃え**
- カード/ボックスで区切られた要素の内部テキストは**中央揃え**
- 日本語のみで簡潔に

## カラーパレット（`css/common.css` の CSS変数）

黄色・オレンジ系の「AI生成でよく見る配色」から脱却し、フォレストグリーン×ストーン
（少し灰みのある紙っぽいベージュ）＋クレイ（テラコッタ寄りの赤茶）に変更済み。

| 変数 | 値 | 用途 |
|---|---|---|
| `--color-bg` | `#f0ede4` | ページ背景（ストーン系） |
| `--color-bg-soft` | `#e5e0d2` | カード内画像の背景など |
| `--color-ink` | `#1a1c16` | 基本テキスト色（黒系） |
| `--color-ink-soft` | `#565a4d` | 補足テキスト |
| `--color-accent` | `#3f5c43` | フォレストグリーン。ナビの下線・ボタン・リンクなど主アクセント |
| `--color-accent-dark` | `#2c4230` | ホバー時の濃いグリーン |
| `--color-clay` | `#a85630` | クレイ（テラコッタ）。**世界地図のピン専用**（下記参照） |
| `--color-white` | `#fffdf8` | カード・モーダルの背景 |

**マスタードイエロー（`--color-mustard`）は廃止済み。今後も追加しない。**

**アクセントカラーは緑(`--color-accent`)の1色に統一済み。** 以前はスポーツページの熱狂パルスや
人図鑑の往来ドット、エジプトの太陽モチーフにもクレイを使っていて「アクセントが2色ある」状態
だったが、チェックリストのレビューで指摘され修正した。**クレイは`world.css`の地図ピンだけで使う**
（地図上でピンを目立たせるための意図的な例外）。新しい装飾要素の配色に迷ったら、まず緑
（または白/ニュートラル系）を検討し、クレイを増やさない。

## タイポグラフィ

- 見出し: **Shippori Mincho B1**（`--font-display`）。柔らかめの明朝体。「単調なゴシックだけだと味気ない」
  という指摘で Zen Kaku Gothic New から変更済み。読み込みウェイトは 500/700/800 のみ（900は未読込なので
  見出しの font-weight は 800 で統一する。900 を指定すると合成太字になり汚くなるので使わない）
- 本文: Noto Sans JP（`--font-body`）。本文は読みやすさ優先でゴシックのまま
- 装飾的な英語見出し（eyebrow）は使わない方針に変更済み

## カードの見た目（角丸+ソフトシャドウを使わない）

以前は strength-card / person-card / sport-card / モーダルがすべて「角丸(`--radius`=18px)＋
ホバーでソフトドロップシャドウ」という、いわゆるSaaSランディングページ的な質感になっていた。
koki-design-taste的にNGなので、**枠線ベース**に統一済み:

- `border: 2px solid var(--color-ink)`、`border-radius: 6px`（モーダルだけ8px）
- ホバーは `box-shadow` ではなく `border-color` を `--color-accent` に変える＋わずかな
  `translateY(-4px)` のみ
- モーダルは浮いて見える必要があるため軽いシャドウ（`0 8px 24px rgba(0,0,0,.18)`）だけ残すが、
  以前の `0 30px 60px` のような重いシャドウには戻さない
- 写真そのものを収める箱（Hero写真、地図、モーダル内の写真）は「カード」ではなく
  「写真のフレーミング」なので `--radius`(18px)の柔らかい角丸のままでよい。角丸+シャドウを
  避けたいのは情報カードのUI的な見た目についてであり、写真の額縁的な丸みまで禁止するものではない
- 新しいカード/ボックスを追加するときも、この2px枠線+radius6pxの言語に合わせる。
  「角丸を大きくする」「ホバーで影を強くする」という方向には戻さない
- グリッドは同じ幅の箱を機械的に並べず（例: `1.25fr 1fr 1fr`）、わずかに非対称にしている
  （トップの強みカード3枚など）
  **注意**: `aspect-ratio` を指定した正方形/固定比率カード（スポーツの写真タイルなど）に
  列幅の非対称(`fr`の差)を適用すると、幅が違う→高さも連動して違う→カードの大きさが
  バラバラに見える、という不具合になる。写真タイルのような「サイズを揺らしたくないグリッド」
  には非対称列幅を使わない（`sports-grid`は`repeat(3, 1fr)`に戻した実例がある）。非対称は
  テキストカード（アスペクト比固定でないもの）にだけ使うこと

## サブページの写真フルブリードHero（world / people / sports）

以前はworld/people/sportsのHeroが単色背景＋抽象モチーフだけで実写がなく、トップページ
（写真ポスター型）とトーンが揃っていなかった。共通コンポーネント `.page-hero--photo`
（`css/common.css`）を追加し、3ページとも実写のフルブリード写真＋暗いグラデーション＋
白文字のHeroに変更済み。

- 使用写真: world→`images/countries/turkey.jpg`（カッパドキアの気球）、
  people→`images/people/p4-ethiopia.jpg`（エチオピアの詐欺師との一枚。最初は
  `p3-india.jpg`だったが指定でこちらに変更）、
  sports→`images/sports/padel.jpg`（パデル仲間との集合写真）
- 各ページの抽象アンビエントモチーフ（地球儀/往来ドット/熱狂パルス）はそのまま残し、
  白系の半透明色（`rgba(255,255,255,…)`）に塗り替えて写真の上でも視認できるようにした

## ページの縦の長さ：PCではスクロール不要を目指す（決定事項）

「各ページとも情報量に対してページが長すぎる、PCで見たときスクロールしなくて済むくらいに
まとめてほしい」という指示があった。**これは「縦スクロールでのストーリーテリング」を
やらない、という決定でもある**（koki-design-tasteのチェックリストで△だった項目だが、
今後はこの方針で確定・対応不要）。

やったこと（1280×900くらいのデスクトップでほぼ収まる/大きな画面なら完全に収まる高さまで圧縮):
- `.page-hero--photo` の `min-height` を `clamp(200px, 30vh, 280px)` まで縮小（以前は
  `clamp(360px, 58vh, 540px)` で明らかに大きすぎた）
- トップページのHero写真のアスペクト比を `3.2/1` まで平たくした（以前は`16/9`→`2/1`と
  段階的に平たくしていった経緯がある。写真の被写体（ピラミッドと本人）が入りきる範囲で
  できるだけ低くする）
- 各セクションの上下パディングは `clamp(56px,10vw,100px)` のような大きい値を持たせない。
  今の基準値は概ね `clamp(14–20px, 3–4vw, 22–32px)` くらいに揃えている
- 地図は`max-width:760px; margin:0 auto`で全体サイズを縮小（`aspect-ratio`固定なので
  幅を絞ると高さも連動して下がる）
- 新しいセクションを追加するときも、まずこの「圧縮された」パディング感覚に合わせること。
  以前の値（`clamp(56px,10vw,100px)`のような大きいパディング）を新しく追加しない

余白を贅沢に使うという好み(koki-design-taste)と「スクロールさせない」という指示は
ある程度綱引きになるが、今回は本人から明確に後者の指示が出ているので、これを優先する。
- DOM構造: `<img class="page-hero__media">` → `<div class="page-hero__scrim">`
  （暗いグラデーション） → `.ambient-bg`（モチーフ、z-index高め） → `.wrap`（本文、最前面）。
  新しいサブページを作る場合もこの順番・クラス構成を踏襲する
- 写真を変えたくなったら、上記3ファイルの`page-hero__media`の`src`を差し替えるだけでよい
  （構造・CSSは共通なので触らなくてよい）

## アンビエント背景（各サブページのpage-hero）

「単調で動きがない」「AIっぽい」を避けるため、各ページのpage-heroに**その場だけの
装飾的な動的背景**を追加済み。クリック等の操作は不要（見た目だけのアンビエント演出）。
`.page-hero` は `position:relative; overflow:hidden`、背景要素は `.ambient-bg`
（`position:absolute; inset:0; z-index:0; pointer-events:none`）、本文側は
`.page-hero > .wrap { position:relative; z-index:1 }` で前面に出す、という共通の仕組み
（`css/common.css`）。`prefers-reduced-motion` では自動的にアニメーション停止。

- `world.html` — 「地球儀が回っている感じ」。右上に配置したワイヤーフレーム球体
  （`.globe`、SVGの円＋楕円）。経線にあたる楕円を `scaleX` で 1→0→-1→0→1 と
  往復させ、位相をずらして4本重ねることで回転しているように見せている
  （`css/world.css` の `@keyframes meridian-spin`）
- `people.html` — 「色んな人が行き交うイメージ」。小さな円（`.walker`）を
  複数、左右にゆっくり流す抽象的な「人の往来」表現（`css/people.css` の
  `walk-left` / `walk-right`）
- `sports.html` — 「人々の熱狂」。複数の円がランダムなタイミングで膨らみながら
  消えていく、歓声・熱気のパルス表現（`css/sports.css` の `@keyframes crowd-pulse`）

新しいページを追加する場合も、同じ `.ambient-bg` の仕組みに乗せて、そのページの
テーマに合ったアンビエント演出を1つ用意する方針。

## トップページ（v2: ポスター型リニューアル）

参考として「プロフィール帳/キャラクターシート」風の画像（手書き風の太い枠線＋番号付きボックス
＋吹き出しの自己紹介シート）を渡されたことがある。番号付きボックスの部分は、以前決めた
「連番ラベルを使わない」というNGパターンと衝突するため、**あえて番号は付けず**、その代わり
枠線を太くする・角にカラータグを付ける、という形で「情報カードとしての力強さ」だけを拝借した。
今後また同種の参考画像が来た場合も、番号や手書き風の装飾までは真似しない方針で進める。

「殺風景でつまらない」という指摘を受けて、以下の構成に変更済み。

- Hero写真は `images/profile/pyramid.jpg`（エジプト・ギザの三大ピラミッドでの環境ポートレート、
  16:9横長）に変更。旧来の証明写真的などアップ(`koki.jpg`)より「旅人感」が出るものを採用
  - 写真の上に名前＋タグライン「旅。野生。世界。」を暗いグラデーション越しに白文字でオーバーレイ
    （ポスター/雑誌の表紙のような見せ方）
  - モバイルでは `aspect-ratio:3/2` + `object-position:32% 58%` にして、ピラミッドと本人の
    両方が見えるように調整済み（4/5など縦長すぎる比率だと人物だけにクロップされるので注意）
- Hero背景に **エジプトモチーフのインタラクティブなアンビエント背景** を追加（`.egypt` /
  `js/index.js` の `initEgyptParallax`）。太陽・ピラミッド2枚・砂丘の4レイヤーをマウス移動に
  応じて視差(パララックス)で動かす。タッチ端末・`prefers-reduced-motion`ではJSが自動でスキップ
- 強みカードの下に「職歴」「趣味・活動」の2枚の情報カードを追加し、トップページだけでも
  経歴と人となりが分かるようにした。職歴は `js/data.js` の `PEOPLE` から本人(isSelf)のcareerを
  参照して描画（two-source-of-truthにならないよう、データの二重管理はしない）
- カードのスタイルは他ページの角丸カードより少し力強く（枠線2px＋角の色付きタグ）して、
  情報量が増えても単調に見えないようにしている
- 「趣味・活動」カードが「職歴」カードに比べて内容が薄く見える、という指摘があったので、
  `WORLD_INTRO.visited`（40）を大きな数字（`.info-card__stat`）で見せてから、タグを並べる
  構成に変更した。`.profile-extra__grid` は `align-items: start` にして2枚の高さを強制的に
  揃えない（揃えると短い方が間延びして見える）。趣味タグに「旅行 40カ国制覇」のような
  数字とかぶる項目を残さない（統計と重複させない）
- タグは `PROFILE.hobbies` を `{label, href}` の配列にしており、`href`がある項目だけ
  `<a>`（新規タブで開く外部リンク）、ないものは`<span>`でレンダリングされる
  （`js/index.js`）。現在「世界一周記YouTube」だけ https://www.youtube.com/@koookiblog
  にリンクしている。他のタグ（スポーツ全般/早起き）はリンクなしでよい

### 「強み」の3枚と「職歴/趣味」の2枚の横幅が合わない、と指摘された件（重要・再発防止）

見た目上は指摘の通り「上の3枚の行」と「下の2枚の行」の境界線が揃っていなかった。原因は
「行全体の横幅」自体は一致していたが、**上が3列グリッド、下が2列グリッドと別々に定義されて
いたため、列の区切り線（内側の境界）が揃っていなかった**こと。行の左端・右端だけ見ると
一致して見えるので気づきにくいバグ。

直した内容: `.profile-extra__grid` を `.strengths__grid` と**まったく同じ列定義**
（`grid-template-columns: 1.25fr 1fr 1fr`）にし、`.info-card--career` に
`grid-column: 1 / 3`、`.info-card--hobbies` に `grid-column: 3 / 4` を指定して、
3列グリッドの上に2枚のカードを乗せる形にした。これで上下の縦の区切り線が完全に一致する。

**今後の教訓**: 縦に並ぶ複数の行（カードグリッドなど）を「揃っているように見せたい」場合は、
行ごとに別のグリッド定義（列数やfr比）を作らない。**同じ列定義を共有し、`grid-column`で
セルを結合する**方式にする。「行全体の横幅が同じだから大丈夫」という考え方はNG
（内側の区切り線が揃わないと人間の目には「揃っていない」と判定される）。
- `PROFILE.meta` は「30歳 / 1996.02.06生まれ / A型 / 水瓶座」に簡略化済み（既婚・早起きは
  外した。早起きは趣味タグ側に移動している）

## トップページのHero写真（旧仕様のメモ）

- 写真は大きめに（`.hero__photo` は最大幅420px、モバイルでも320pxまで許容）
- アスペクト比は正方形(1:1)ではなく `3/4` の縦長ポートレート比。正方形だと
  クロップが強すぎて窮屈な印象になるため
- Hero と 強みブロックの間の余白が二重に効いて間延びしていた問題があったので、
  `.hero__inner` の下パディングを0にし、`.strengths` の上パディングだけで
  余白を作るようにした（両方に大きいパディングを持たせない）

## インタラクション（実装済み・これで十分という結論）

以下で「インタラクティブ」の要件は満たしている。追加の要望（カーソル追従アイコン、
ポストカード風フリップ、数値カウントアップ、進行度バーなど）は**明確な指示があるまで追加しない**
（一度提案したが、追加不要という回答だった）。

- スクロールフェードイン（`.reveal` / `js/main.js` の `initReveal`）
- 世界地図のピンクリック → モーダルでエピソード表示（`js/world.js`）
- 人図鑑の横スライドカード（`js/people.js`、矢印ボタン＋スクロールスナップ）
- スポーツ画像のホバー拡大＋クリックで詳細モーダル（`js/sports.js`）

## 2026-08-20: 現在の状態（別セッションはまずここを読む）

### 世界観
- サイト全体が**ほぼ黒の夜空**（`.space`）。光源は暖色ひとつだけ。星は種を固定した乱数で配置。
- **惑星は実写を丸く切ったもの**（`.orb`）。CSSで描いた光る球ではない。
  トップの3惑星の写真は `CosmosHome.tsx` の `PLANET_PHOTO`。
- 移動は**レトロなブリキのロケット**（`Rocket.tsx`）。`WarpLink.tsx` で押すと
  ロケットが飛んでから遷移する（`.warp` / 1100ms）。戻る導線は `ReturnToSystem.tsx`。
- ヘッダーはワードマークのみ（タブは廃止）。移動はトップの星系に一本化。
- BGM: `BgmPlayer.tsx`（宇宙海賊 / もりつぐ）。z-120 でゲーム中も操作できる。

### ページ名
- トップ「Who am I ?」／「あぁ素晴らしき地球」／「世界のクセ強人類」／**「スポーツの惑星」**
- プロフィール行（年齢・血液型など）と、各ページの小さなリード文は**削除済み**。
  「読まれないから」という理由。復活させないこと。

### ゲーム（5種・操作の質を全部変えてある）
| 場所 | ファイル | 操作 |
|---|---|---|
| ゲート | `ShootingGameGate.tsx` | 左右移動で狙う（自機は**人**） |
| 世界地図 | `games/FlightGame.tsx` | 戦闘機を操縦して輪をくぐる |
| 人図鑑 | `games/MemoryGame.tsx` | 光った順を記憶して押し返す |
| スポーツ | `games/SprintGame.tsx` | 連打で走る＋ハードルを跳ぶ |
| 職歴 | `games/CareerRunGame.tsx` | 壁のすき間を抜ける（残機なし） |

- 難易度は**項目ごと**（`data.ts` の `level: 1〜5`）。★で挑戦前に表示。
  ゲーム側の `tuning(level)` が時間・速度・的の大きさを変える。
- 世界地図はピンを押すと**その国へズームしてから**ゲームが開く。地図にペンギン/クジラ/ラクダ。
- クリア時は `Modal` の `celebrate` で閃光・光の筋・粒・紙吹雪。

## 2026-08-20(2): オープニングを作り直した + 夜空の色の不整合を潰した

「最初の背景が緑なのが気に入らない、スターウォーズ感にしたい」「全てに根拠をもって、
整合性を、見た人が終始ワクワクするように」という指示を受けての作業。

### オープニング（`ShootingGameGate.tsx` の `welcomeOverlay` / `.opening__*`）

**スターウォーズから借りたのは見た目ではなく三拍子の構成**（DESIGN.mdの
「参考をそのまま真似ず、狙いだけ抜く」に従う）:

1. 静寂の一行「ずっと前、はるか遠くの国で──」（生成り白・0〜3.4秒）
2. ワードマーク「Who am I ?」が現れて奥へ引く（3.2〜6.8秒）
3. 前口上が奥へ流れていく（クロール・3.9秒〜、17秒かけて通過）
4. 手前に「ゲームスタート」が浮かぶ（6.6秒）

決めたことと、その根拠:

- **黄色(#FFE81F)とNews Gothicは使わない**。パレットに黄色を入れないという決定が
  あるため、クロールの色は既にパレットにある `--color-ember`（#d9a86a、夜空側の
  唯一の光源色）、書体はサイト共通の明朝 `--font-display`。
  → 「スターウォーズの黄色」ではなく「この夜空の中で光る文字」として成立させる。
- **背景は `--color-space`（#090a09）**。以前は `--color-ink`（#1a1c16）で全面を
  塗っていた。これは本文用の黒で緑に寄っており、**オープニングの空と本編の夜空が
  別の黒だった**（これが「最初の背景が緑」の正体）。トークンを新設して一本化。
- **星は本編と同じ `.space__star`**（`SpaceBackdrop` に地平線なしの `scenery="void"`
  を追加）。オープニングと本編で空を地続きにするため、星を別実装しない。
- **光源はクロールの文字ひとつだけ**。`text-shadow` のグローは足さない。
- **ボタンは緑のピルをやめた**。globals.css自身に「緑は地上のページ用」と書いてあり、
  夜空側は ember/nebula/clay。加えて丸いピル型は「丸くしてふわっとさせる」NGに当たる。
  → ember の2px枠線＋6px角丸（サイト共通のカード言語）、ホバーで色を反転。
- **待たせない**。画面のどこかを押す/キーを叩くと、前口上の途中でもボタンが即出る
  （`.opening__start--now`）。`prefers-reduced-motion` では動きを全部止めて静止した
  文章＋ボタンにする。

実装上ハマった点（再発防止）:

- **文字を「画面座標」で動かしてはいけない**。`top` をアニメーションさせると、
  perspective の効いた手前側が破綻して巨大化・画面外へ溢れる。正しくは
  **傾いた面（`.opening__plane`、静止）の上を `translateY` で滑らせる**
  （`.opening__crawl`）。この2枚に分けておくと、傾き・遠近・幅をメディアクエリで
  変えても keyframes を書き換えなくて済む。
- **消え際に `mask-image` を使ってはいけない**。マスクは変形前の自分の箱に貼り付くので
  「奥に行くほど薄く」ではなく「最初と最後の行だけ薄い」になる。画面座標の膜
  （`.opening__veil`）を被せるのが正解。かつ **膜は perspective の外**（`.opening__stage`
  の兄弟）に置く。3D空間の中に入れると、手前に来た文字の方が膜より前に描画される。
- スマホは面を狭く（72vw）・遠近を弱く（760px）し、**消失点を画面上端から20vhに下げる**。
  上端に貼り付けると帯の下に大きな空白ができて構図が抜ける。
- CSSを部分置換したとき **古い `@keyframes` が残って後勝ちしていた**事故があった。
  同名の keyframes が2つあると後ろが勝つので、置換後に `grep -c` で数を確かめる。

### 夜空の黒の統一と、残っていたNGパターンの撤去

- `--color-space: #090a09` を新設し、`.space` のハードコードもこれを参照。夜空の上に
  直接置く面（ヘッダー/フッター/ゲーム画面の全面/惑星/戻る導線/地図のツールチップ）を
  すべてこの黒に寄せた。写真の上に乗るスクリム（人図鑑カード・BGM・HUD）は
  `--color-ink` の半透明のままでよい（見え方が変わらないため）。
- **`ReturnToSystem.tsx` にピンクの外側グロー `rgba(216,111,168,.4)` が残っていた**。
  #d86fa8 はDESIGN.mdが名指しでNGにしているキャンディ色で、しかも外側グロー
  （「光らせて逃げる」）。撤去した。
- **装飾的な英語ラベルが2つ残っていた**: トップの「COLLECTION」（集めた記憶ボタン）と
  戻る導線の「RETURN」。どちらも削除し、日本語だけにした。

## 2026-08-20(3): 夜への統一・ember一本化・惑星の重み付け・賞品の告知

### ゲームの中も夜にした（3ゲームすべて）

`three-kit.ts` に **`nightSkyTexture()` と `addNightLights()`** を追加し、
`ShootingGameGate` / `FlightGame` / `CareerRunGame` の3つとも空・霧・光・地面色を
これに統一した。**ゲームロジック（速度・間隔・判定・勝敗のしきい値）は一切触っていない**。

- 空は `#05060a → #0a1017 → #1b2530` の縦グラデ＋種を固定した星＋**地平線ぎわの ember の残光ひとつ**。
  各ゲームで空を自作すると「ゲームだけ別の空」になるので、必ずこの関数を使う。
- 残光の高さ（`glow`）は**その画面の地平線の位置に合わせる**。既定 0.5、ゲート 0.46、
  フライト 0.54、職歴 0.5。ここを画面外（0.9など）にすると、ただ真っ黒な画面になる。
- 砂は2段持ち（`PAL.sandNight` #4d3b26 / `sandNightFar` #3a2c1c）。暗い方だけだと
  起伏が読めない。
- ライトは月光（淡い青白）を主、地平線の残光を弱い暖色のフィルとして反対側から1灯だけ。
- ゲートに残っていた**パレット外の原色**（空 #ff8a4c〜#ffe08a、砂 #e3a75c、弾 #ffe066＝黄、
  目印 #ff6f45、自機の服 #39a862）を全部トークン側の色に置き換えた。弾は ember
  （＝その画面の光源と同じ色。光り物を増やさない）。

### 暗い面の上のアクセントは ember 一色（緑をやめた）

「宇宙空間の緑はイメージが湧かない」という指摘。`globals.css` に **`.btn-ember`**
（2px枠線＋6px角丸、ホバーで塗り反転）と `.btn-ember--solid`（その画面の主導線だけ塗る）を
用意し、夜空の上の緑のピル11個をすべてこれに置き換えた。

- `text-(--color-accent-light)`（薄緑）→ `text-(--color-ember)`、
  `text-(--color-accent-dark)` → `text-(--color-nebula)` に一括置換。
- **世界地図だけは明るい紙面**なので ember では沈む。到達済み＝クレイ、未到達＝薄い墨
  （`--color-ink`/35）にして、ここからも緑を外した。
- 結果、**`--color-accent`（緑）はトークンとしては残っているが実装では未使用**になった。
  今後、明るい紙面のUIを作るときだけ使う候補として残しておく。
- パレット外の**青白いグロー**も撤去: `planet-breathe` の `rgba(146,190,224)` と
  `.orbit` の枠線 `rgba(150,196,226,.2)` を ember 側の値に変更。

### 惑星は「中身の数」で大きさが決まる（`CosmosHome.tsx`）

「世界地図はコンテンツが多いから大きく、輪も。活動は3つだけだから小さく」という指示。
`planetSize(count)` で **直径 = 58 + 件数 × 6.5px** とし、`data.ts` の件数から自動で決める
（国を1つ足せば惑星が育つ＝集めた量がそのまま大きさになる）。

- 軌道の割り当ても件数順にした。内側から **職歴1 → 趣味3 → 人4 → スポーツ6 → 国9**。
  大きい惑星が外周をゆっくり回る形になり、同時に「最内周の半径 − 惑星の半径 > 太陽の半径」
  という既存の制約も自然に満たす。
- **輪（`.planet-ring`）は件数の多い2つだけ**。国（9件）は二重、スポーツ（6件）は一重。
  輪は惑星の直径に対する％で描くので、惑星が育つと輪も育つ。
  奥半分を惑星の後ろ（`z-index:-1`）、手前半分を `clip-path: inset(50% 0 0 0)` で惑星の前に
  重ねて、本当に貫通しているように見せている（全部前に描くと輪をかぶせただけの絵になる）。
- **系の大きさは幅だけで決めてはいけない**。惑星が育った結果、外周の惑星が画面の下で
  切れた。`w-[min(86vw,52vh,520px)]` と**高さも制約に入れる**。
- **太陽の大きさは系に対する％で持つ**（`w-[25%]`）。px固定（旧 `clamp(74px,20vw,144px)`）
  のままだと、系が縮んだときだけ太陽が相対的に大きくなり、最内周の惑星のラベルが
  太陽の写真に乗る。
- 最内周は 46% → **58%** に広げた。「半径 − 惑星の半径 − ラベルの高さ > 太陽の半径」を
  満たす必要がある（ラベルの高さを勘定に入れ忘れると文字だけが太陽に乗る）。

### 豪華賞品の告知（2箇所）

- **集めた記憶パネルの先頭**（＝「続けるか」を決める場所）に
  「全部クリアして豪華賞品をゲット！！ あと○個」。集めきると
  「全記憶を回収。こうきから豪華賞品を進呈します」に変わる。
- **オープニングの前口上**にも1ブロック追加（「すべての記憶を集めた者には、
  こうきから豪華賞品が贈られる。」）。最初に目的が分かる方が遊ぶ動機になる。

## 2026-08-21: 世界地図のゲームを3種類に分けた（バトル実装）

「9カ国が全部フライトゲームで飽きた」への対応。**どのゲームを開くかは `data.ts` の
`game`（`"battle" | "balloon" | "iceflow"`）が持つ**。ページ側で条件分岐を増やさない。

### エンカウントバトル（7カ国・`games/BattleGame.tsx`）

FFのATBの「骨格」だけを借りた。①時間は常に流れている ②ゲージが満ちて初めてコマンドが開く
③選ぶとゲージが空になる。**Three.jsは使っていない**（主役は実写と明朝の文字で、立体である
必要がない。DOM+CSSなら文字が滲まずスマホでも軽い＝MemoryGame/SprintGameと同じ判断）。

覚えゲーにしないための2層:
- 相手ごとに**効く一手がひとつだけ**ある（発見の快感）。それは `data.ts` の `kind:"weak"` で、
  **その国で清水さんが実際にやったこと**。例: ケニアの物乞い→「仲良くなる」、
  アルゼンチンのタトゥー軍団→「脱ぐ」、ナミビアの猿→「諦める」。`story` から取っていて、
  作り話は足していない
- 敵は攻撃前に**予告**を出す。その間だけ `kind:"guard"` が有効で、予告中に殴りにいくと
  倍もらう（反応の緊張）。だから殴り続けるだけでは勝てない

- 難易度は `tuning(level)` が 敵HP / 敵の攻撃間隔 / 予告の長さ / ゲージ速度 を動かす
- 色の役割を分けた: **ember = 自分の一手、clay = 危険（予告・被弾）**
- **背景写真は「舞台」に落とす**（`brightness(.34) saturate(.62)` ＋ 下から闇を上げて足元を作る）。
  明るいままだと写真の中の顔が主役に見えて、敵と自分が「写真に貼ったシール」になった
- **背景に向くのは「場所」の写真で、自撮りは向かない**（インドが自撮りで、顔が2つ競合している）
- 自分は立ち姿の実写を枠付きの札にして置く（切り抜かないので粗が出ない）。風景ごと写って
  いるので、枠の中で `scale(1.6)` して人物に寄せている
- **ゲーム内の画像は必ず先読みする**（`priority` / `loading="eager"`）。既定の遅延読み込みだと
  押した瞬間に開くゲームで最初の数秒が黒くなる（実際になった）。この画面は開くまで一度も
  要求されないので、初期表示は重くならない
- 背景写真が無い国（ケニア・ナミビア）は、その国の記憶写真で代用する。存在しないパスを
  `/_next/image` に渡すと **400** が返って真っ黒になるので、置いた分を
  `HAS_BATTLE_BG` に明示的に持つ

### スマホで指と被る問題（恒久ルール）

原因は推測ではなく実装。`FlightGame.tsx` が**指の絶対座標をそのまま機体の位置にしていた**
（`touchLift = 2.1` はその応急処置の跡）。今後のゲームは必ず次のどれかにする:

1. **相対ドラッグ**（触った点からの移動量で動かす）
2. **操作帯の分離**（画面下の枠だけ触る。バトルがこれ）
3. **位置を問わない操作**（どこを押しても同じ。気球のバーナーがこれ）

### バトル背景の割り当て（`public/images/battle/{国id}.jpg`）

**背景に向く写真の条件**: ①「場所」が写っている（自撮りは向かない。写真の中の顔が
主役に見えて、敵と自分が写真に貼ったシールになる） ②暗い、または余白がある
③被写体が地平線あたりに小さく並ぶ（前に立つ敵と自分に競合しない）。

- ケニア ← もらった写真の「アフリカ 南米とか」（サバンナの地平線に象の列）。
  広い舞台で、動物が地平線に並ぶだけなので前面と競合しない
- ナミビア ← 「アフリカ 中東」（塩湖の夕暮れのシルエット）。暗くて暖色、
  そのままサイトのパレット（emberの地平線＋暗い地面）に一致する
- インドは自撮りだったので「インド背景」に差し替え
- `object-position: 50% 62%` で**空より地面側を見せる**。真ん中で切ると空だけが写って
  何の場所か分からなくなる（ケニアの象がまさにそうなった）

### 「初回クリア率 ○○%」（全ゲーム共通・`lib/difficulty.ts`）

★だけだと難しさが体に来ないので、パーセントに翻訳して挑戦前に見せる。
`GameShell` の★の下と、ゲートの説明画面に出る。

**数字の性質を混同しないこと**:
- **ゲートの55%だけが実測**。`tools/sim-gate.mjs` の初心者モデルで計測した値
- **他は level から出した「設計上の想定値」**（1→62% / 2→48% / 3→35% / 4→23% / 5→12%）。
  ★と矛盾させないため level の関数として一箇所で決めている（項目ごとに勘で置くと
  ★4より★5の方が高い、という事故が起きる）
- 実測に切り替える道筋: GA4 が動けば項目ごとに `game_start` と `game_clear` が溜まるので、
  「初回の挑戦で勝てた人 ÷ 挑戦した人」を `MEASURED` に入れる（Supabase から取る形にすれば自動更新）
- 各ゲームには `itemId`（解錠キー）を渡してある。実測値はこのキーで引く

### トルコ: カッパドキアの気球（`games/BalloonGame.tsx`）

調べた事実をそのまま設計にしている。
- **日の出前に離陸する** → 門を1つ抜けるごとに `nightSkyTexture({dawn})` の dawn を上げ、
  空が夜からemberの朝焼けへ変わる。**進捗を数字ではなく空の色で見せる**
  （混ぜ先の色も土とemberの色域から出さない。紫やピンクは入れない）
- **妖精の煙突** = 硬い玄武岩が帽子のように柔らかい凝灰岩の胴を守っている形。
  胴（テーパーした円柱）＋一回り太くて暗い帽子で作る。**ただの円柱にしないこと**。
  この一段があるだけで「あの奇岩」に見える
- **同時に100機以上の気球が飛ぶ** → それを「天井」として障害物にした。
  下は煙突、上は他の気球、その隙間を抜ける
- 操作は「押している間だけバーナー、放すと下降」の一本。**画面のどこを押しても同じ**なので
  指が自機に被らない（指被り問題の解決策③）

**物理を気球らしく鈍くした（重要）**: 最初は LIFT 26 / GRAVITY -15 だったが、
これは気球ではなくジェットの動きで、**人の反応（0.2秒遅れ）では振動して抜けられない**。
シミュレータで初心者モデルが「1つ目の門で大きく外す」失敗を200回中145回していた。
LIFT 12 / GRAVITY -6.5 ＋ 空気抵抗（毎フレーム0.985）＋ 終端速度6 に変更。

### 難易度は測ってから決める（`tools/sim-balloon.mjs`）

ゲートと同じやり方で、ゲームのロジックを再現したシミュレータを用意した。

```bash
node -e "import('./tools/sim-balloon.mjs').then(m=>{for(const l of [1,2,3,4,5]) console.log(m.report(l))})"
```

| ★ | 実測(初見) | 表示している値 | 慣れた人 |
|---|---|---|---|
| 1 | 60.8% | 61%（実測を採用） | 99.5% |
| 2 | 43.7% | 48% | 98.7% |
| 3 | 27.0% | 35% | 93.8% |
| 4 | 19.2% | 23% | 82.2% |
| 5 | 8.8% | 12% | 69.0% |

**プレイヤーモデルの作り方でハマった点（再発防止）**:
- **不感帯（`dead`）が無いモデルは人間ではない**。毎フレーム焚く/放すを切り替えると
  浮力と重力の間で激しく振動し、隙間をいくら広げても勝率0%になる。人は「ある程度
  ずれてから直す」のでこの項が必須
- **先読み（`antic`）も必須**。位置だけ見て操作するモデルは人間よりはるかに下手で、
  そのままだと「異常に難しいゲーム」と誤判定する
- ブラウザ越しの計測は避ける。この環境は `requestAnimationFrame` が止まるので、
  rAFを差し替えて手回しする必要があり遅い。さらに**勝つと「見る →」しか出ない**ので
  「もう一度」を押す前提のハーネスは2回目以降が全部勝ち扱いになる（実際にこれで
  0%/100%というでたらめな数字が出た）。**勝率はNodeのシミュレータで測ること**

### 南極: パックアイスを渡る（`games/IceflowGame.tsx`）

調べた事実をそのまま設計にしている。
- **パックアイス**（浮き氷が海流で積み重なったもの）が足場、**ポリニヤ**（風や波で
  分散してできる開水面）が落ちたら終わりの隙間
- 遠景は**テーブル型氷山**（棚氷が割れたものなので上面が平ら）。
  **とんがった山にすると南極ではなくなる**のでボックスで平頂に描く
- 操作は「押して踏み込みを溜め、放して跳ぶ」。**足りなくても跳びすぎても落ちる**ので
  「強く押せばいい」にならない。他の4つ（左右移動・連打・記憶・コマンド）と
  操作の質が被らないようにこれを選んだ
- 画面のどこを押しても溜まるので指が自機に被らない（解決策③）
- 実装: 自分は常に x=0 に立っている見立てで、着地したら**世界の原点を新しい足場に移す**
  （自分を動かすと、無限に伸びる座標を扱うことになる）
- **挑戦前の画面でも足場を組んでおく**こと。`reset` をゲーム開始時だけ呼ぶと、
  intro では人が海の上に浮いて見える（実際にそうなった）

難易度は `tools/sim-iceflow.mjs` で実測:

| ★ | 実測(初見) | 表示 | 慣れた人 |
|---|---|---|---|
| 1 | 60.0% | 62% | 100% |
| 3 | 31.1% | 35% | 100% |
| 5 | **12.3%** | 12%（実測を採用） | 100% |

このゲームのモデルは「距離の見積り誤差(`aimErr`)」と「指を離すタイミングの誤差(`timeErr`)」の
2つだけで書ける。`chargeMs` を短くすると、同じタイミング誤差が大きな距離の誤差に化けるので
難しくなる、という関係になっている。

### 効果音（`lib/sfx.ts`）

**3つだけ**。理由: 「FFっぽさ」は音の本数ではなく**鳴るタイミング**で出る。
命中（手応え）／被弾（焦り）／クリア（解放）の3拍だけ押さえれば足りる。
増やすと画面がうるさくなり、「動きは意味のある1つに絞る」という方針と喧嘩する。

| 音 | ファイル | 鳴る場所 |
|---|---|---|
| 命中 | `audio/sfx/hit.mp3` | バトルで自分の一手が当たった時 / ゲートで敵を撃破した時 |
| 被弾 | `audio/sfx/damage.mp3` | バトルで殴られた時 |
| クリア | `audio/sfx/clear.mp3` | **GameShell の `phase === "won"`** ＝全ゲーム共通で1箇所 |

- **ゲートの自動発射に音は付けていない**。240msごとに撃つので、1発ごとに鳴らすと
  機関銃のようになって手応えが逆に消える。当たった時だけ鳴らす
- 鳴らすのは `pop()`（ダメージ数字を出す関数）の中1箇所にした。数字と音は必ず同時に
  出るべきものなので、別々の場所に書くと片方だけ出るズレが生まれる
- **消音はBGMのスピーカーボタンと同じ状態を見る**（localStorage の `koki-bgm-muted`）。
  別々に持つと「消したのに効果音だけ鳴る」ことになる
- 同時再生は `cloneNode()` で複製して鳴らす。同じ要素を使い回すと前の音が切れて
  プツプツした鳴りになる
- ゲームを開いた時に `preloadSfx()` で先読みする。押した瞬間に鳴らないと手応えが消える

**使わなかった素材**: `iQb8Pav3sAY5f74Sj5Z2CkJyvA.zip` は FINAL FANTASY XIV の公式
サウンドパック（47ファイル、スクウェア・エニックスの著作物）だったので、公開サイトには
入れていない。FF感は音そのものではなく鳴るタイミングで作る、という方針で代替した。

### 世界地図の9カ国 = 3種類のゲーム（完成）

| 国 | ゲーム | 操作 |
|---|---|---|
| ケニア/アルゼンチン/ナミビア/ペルー/ヨルダン/ブラジル/インド | `BattleGame` | 下部のコマンド枠を押す |
| トルコ | `BalloonGame` | どこでも長押しで上昇 |
| 南極 | `IceflowGame` | どこでも長押しで溜めて跳ぶ |

旧 `FlightGame`（輪をくぐる）は9カ国すべてが上記に置き換わったので**繋がっていない**。
ファイルは残してあるので、使いたくなったら `data.ts` に `game: "flight"` を足して繋ぎ直す。
- 効果音（命中・被弾・クリア）は未実装。素材待ち
- 既存のフライト/ゲートを上記ルール①に直す作業

### 次に相談したいこと（未決）

- **トップの惑星のうち「歴」「趣」だけが実写ではなく黒い円＋漢字**。
  「惑星は実写を丸く切ったもの」という決定から外れている。当てる写真がないため未対応。

### 未着手
- **Supabase は未使用**。当初アクセス計測用に用意したが着手していない。
- GitHub リポジトリは**公開**。認証情報の混入はなし（履歴も確認済み）。

## 画像の命名・配置ルール

`js/data.js` が画像パスの唯一の参照元。フォルダ構成:

```
images/profile/koki.jpg          トップの本人写真
images/countries/{id}.jpg        世界地図の国別写真（id は data.js の COUNTRIES[].id）
images/people/{id}.jpg           人図鑑の写真（p1〜p4、p1は images/profile/koki.jpg を共用）
images/sports/{id}.jpg           スポーツ写真（data.js の SPORTS[].id）
images/world-map.svg             世界地図ベース（Wikimedia Commons, パブリックドメイン）
```

新しい国・人物・スポーツを追加する際は `js/data.js` の該当配列にオブジェクトを1つ追加し、
写真をこの命名規則でフォルダに置くだけで反映される（HTML/CSS修正は基本不要）。

## 画像圧縮ルール

スマホ撮影の写真は4000px級・数MBになるため、必ず圧縮してから配置する。
macOS標準の `sips` で以下のように長辺1600px・JPEG品質72に統一している
（47MB → 8.9MBまで削減した実績あり）。

```bash
sips -Z 1600 -s format jpeg -s formatOptions 72 "元ファイル" --out "出力ファイル"
```

PNGで受け取った写真（透過が不要なもの）もこの手順でJPEGに変換して統一する。

## Hero写真とキャプションだけ左右の余白が消えていた事故（`padding`ショートハンドの罠）

トップページのHero写真とその下のキャプション行だけ、左右の余白が0になって「強み」以下の
コンテンツと左端が揃わない、という指摘があった（写真が左右フルブリードで、見出し/カードは
インセットという、意図しない不揃い）。

原因: `.hero__inner`（`.wrap`と同じ要素に付けている）に

```css
.hero__inner {
  padding: clamp(16px, 3vw, 24px) 0 0;
}
```

という**`padding`ショートハンド**を書いていた。`padding`ショートハンドは指定しなかった辺も
含めて4辺すべてを上書きする。「上だけ変えたい」つもりで書いても、右・左・下は明示的に`0`に
なり、`.wrap`が設定していた左右の余白（`var(--gutter)`）を丸ごと消してしまう。

直し方: 特定の1辺だけ変えたいときは、`padding`ショートハンドではなく
**`padding-top`のような個別プロパティ**を使う。

```css
.hero__inner {
  padding-top: clamp(16px, 3vw, 24px);
}
```

**今後の教訓**: 既存の要素（特に`.wrap`のような共通クラスと組み合わせている要素）に
「一部の辺だけ」余白を追加/変更したいときは、`padding`/`margin`のショートハンドで
書かない。個別プロパティ（`padding-top`など）を使うか、どうしてもショートハンドで
書くなら4辺ぜんぶを意図通りの値で明示する。「変えたい辺だけ書けば他は元のまま」には
ならない、という点を常に疑うこと。

同時に、Hero写真下の「エジプト・ギザの三大ピラミッドにて」のようなキャプションは
説明過多NGパターンに触れるとの指摘で削除した（`PROFILE.photoCaption`は廃止）。

## 地図フレームの背景色（水色グロー事故の記録）

`.map-frame` にかつて `background:#cfe3df`（水色）＋`box-shadow: 0 24px 48px …`（大きくボケた影）
を指定していたところ、角丸でクリップした四隅からその水色がにじんで見え、「水色のボワっとしたやつが
ダサい」と指摘された。地図の四隅がSVG画像の四角形からborder-radiusで丸められる際、画像の角と
枠の丸みの間にわずかな隙間ができ、そこから背景色が漏れて見える。直した内容:

- `background` を `var(--color-bg-soft)`（ストーン系ニュートラル）に変更
- `box-shadow` を廃止し、他のカードと同じ `border: 2px solid var(--color-ink)` に変更
- 今後、画像を角丸コンテナに入れるときは、コンテナの背景色をパレットのニュートラル系に
  揃える（写真と無関係な目立つ色を背景に敷かない）。ソフトシャドウも使わない
  （カードの見た目の方針と同じ理由）

## 世界地図のピン座標について

`images/world-map.svg`（Wikimedia Commons "BlankMap-World-Equirectangular"）上の
各国パス（`id="ar"` などISO 2文字コード）の中心座標をブラウザの `getBBox()` で実測し、
viewBox に対する%に変換して `js/data.js` の `COUNTRIES[].x / .y` に格納している。
南極(`aq`)を含む必要があるため、Antarcticaを含まない「compact」版ではなく
equirectangular版を採用した。新しい国を追加する場合も、同じ方法（ブラウザで対象国の
`getBBox()` を実測）で座標を出すのが最も正確。
