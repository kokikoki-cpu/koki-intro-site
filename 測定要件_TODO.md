# 測定要件 TODO（GTM / GA4 / Looker Studio）

作成 2026-08-24。**この文書は計画のみ。実装は何もしていない。**

---

## 0. 今どこまでできているか

過去のセッションで土台だけ敷かれている（コミット `d50dc2b`「lay the GTM groundwork」）。

| もの | 状態 |
|---|---|
| `src/lib/track.ts` | **できている。** dataLayerに積む唯一の窓口。イベント15種をTypeScriptの型で固定済み |
| `src/components/TrackClicks.tsx` | **できている。** `data-track`属性を1つのリスナーで拾い `cta_click` を送る |
| `layout.tsx` のGTMスニペット | **入っている。** `NEXT_PUBLIC_GTM_ID` があれば起動する作り |
| GTMコンテナ | **未作成** |
| GA4プロパティ | **未作成** |
| `NEXT_PUBLIC_GTM_ID` | **未設定**（`.env.local`にもVercel環境変数にも無い）→ 本番HTMLに`googletagmanager`は出ていない＝完全休眠 |
| `data-track` の付与 | **9箇所だけ。** ほぼゲーム内のボタン。ヘッダー/強みカード/地図のピン/ロケット等は未付与 |

**土台はある。線がまだ繋がっていない。**

---

## 1. 役割分担（ここを最初に合わせたい）

GTM・GA4・Looker Studioの設定は**すべてブラウザのGUI操作**で、CLIからは触れない。

- **清水さんがやる**：Googleアカウントでの各種作成、GTMの画面設定、GA4の画面設定、Looker Studioの作成、UUを集める
- **Claudeがやる**：コード側の実装、dataLayerの設計、**GTMの画面で何をどう押すかの手順書を書く**、検証（本番HTMLにタグが乗ったか、dataLayerに正しい形で流れているか）

手順書を書く → 清水さんが画面で押す → Claudeが結果を検証する、の往復が一番速い。

---

## 2. 前提作業（これが終わらないと①〜⑥が一切動かない）

- [ ] GA4プロパティを作る → 測定ID `G-XXXXXXXXXX` を取得
- [ ] GTMコンテナ（ウェブ）を作る → コンテナID `GTM-XXXXXXX` を取得
- [ ] GTMに「GA4設定タグ」を1つ置く（測定IDを入れる、All Pagesで発火）
- [ ] **GA4の拡張計測で「履歴の変更に基づくページビュー」をONにする**（SPAなので必須・懸念5）
- [ ] **Googleシグナルはオフにする**（データしきい値対策・懸念4）
- [ ] `NEXT_PUBLIC_GTM_ID` を Vercel の環境変数に登録（Production / Preview / Development）
- [ ] `.env.local` にも同じ値を入れる（ローカル検証用）
- [ ] 再デプロイ → 本番HTMLに `googletagmanager` が乗ったことを確認（Claudeが確認可）
- [ ] GTMプレビューモードで、既存の15イベントがdataLayerに流れているか確認

---

## 3. 要件ごとのTODO

### ① スクロール率 → GA4

> ⚠️ **このサイトはPCでスクロールが起きない設計。先に「懸念1」を読んでほしい。**

- [ ] GA4の拡張計測「スクロール数」は**90%固定**なので使わず、25/50/75/100%はGTMで自前に作る
- [ ] GTM「スクロール距離」トリガー（縦方向・割合・25,50,75,100）
- [ ] **SPA対策**：`router.push()`遷移ではスクロールトリガーがリセットされない。History Changeで初期化する仕組みを入れる
- [ ] GA4イベントタグ `scroll_depth`（パラメータ：`percent_scrolled`, `page_path`, `device_type`）
- [ ] 滞在時間との組み合わせ：GA4が自動で送る `engagement_time_msec` を使うか、GTMのタイマートリガー（15s / 30s / 60s）と掛けて複合イベントにするかを決める
- [ ] カスタムディメンション `percent_scrolled` を登録

### ② ボタンクリック → GA4

**ここが一番進んでいる。** `TrackClicks.tsx` の仕組みが既に動いているので、属性を付けるだけ。

- [ ] `data-track` を主要ボタンへ付ける（現状9箇所→下記を追加）
  - ヘッダーのナビ4つ
  - トップの強みカード3枚
  - ロケット（WarpLink）
  - 世界地図のピン9個
  - 人図鑑のカード送り（前へ/次へ）
  - スポーツのタイル6個
  - BGMのオン/オフ
  - オープニングの「音を出す」「ゲームスタート」
  - 合言葉でのスキップ
- [ ] GTMでカスタムイベントトリガー `cta_click`
- [ ] dataLayer変数 `track_key` / `label`
- [ ] GA4イベントタグを作成
- [ ] **報告書に書くこと**：要件は「class, id等を使った効率的な計測」だが、**`data-track`属性方式の方が堅い**。classやidは見た目の都合で変わるが、`data-track`は計測専用なので壊れない。要件の意図（＝1つ1つ手で設定しない）は満たしつつ、より良い方法を採ったと説明する

### ③ キーイベント（3ページ以上遷移したユーザー）

- [ ] GTMのカスタムHTMLタグ or カスタムJavaScript変数で、sessionStorageにページビュー数を数える
- [ ] 3ページ目に達した時、`three_pages_viewed` を**同一セッションで1回だけ**発火
- [ ] **SPA対策必須**：History Changeトリガーで数える（`router.push()`のため）
- [ ] GA4管理画面で `three_pages_viewed` を**キーイベント**としてマーク
- [ ] 懸念2も参照（ゲートのせいで達成が難しい）

### ④ GTM変数 → GA4

選定案（「変数で返した方が望ましいデータ」として）：

- [ ] `visit_count` — 訪問回数。1st Party Cookie か localStorage で自前カウント
- [ ] `entry_page` — そのセッションで最初に着地したページ（sessionStorage保持）
- [ ] `referrer_host` — 経由元のホスト名。**SPAなので初回しか取れない → sessionStorageで持ち回る**
- [ ] `pages_in_session` — セッション内の閲覧ページ数（③と共用）
- [ ] `gate_cleared` — **ゲートを突破したか。このサイト固有だが、離脱分析に一番効く**
- [ ] `device_type` — GTM組み込み変数で足りる
- [ ] 各変数をGA4のカスタムディメンションに登録（イベントスコープ / ユーザースコープを使い分け）

### ⑤ Looker Studio ダッシュボード（1ページ以上）

- [ ] GA4コネクタで接続
- [ ] 1ページに載せる案：
  - UU / セッション / キーイベント達成数（**要件⑦の証跡にもなる**）
  - ページ別スクロール到達率（device_typeで切る）
  - ボタンクリックの多い順（`track_key`別）
  - **ゲート突破率・ゲーム別クリア率**（このサイトの本題。一番面白い数字になる）
  - いいね / シェア数（⑥と連動）
- [ ] **データが溜まってから作る**（空では作れない・懸念7）

### ⑥ カスタムディメンション / カスタムメトリクス（いいね・シェア）

> ⚠️ **いいねもシェアも、今このサイトに存在しない。機能の実装から始まる。懸念3を参照。**

- [ ] いいねボタンを実装。方式を決める：
  - **(a) localStorageのみ** — 自分の端末に残るだけ。総数は見えない。バックエンド不要・最軽量
  - **(b) Supabase等で永続化** — 総数が見える。DESIGN.mdに「Supabaseを軽量機能用に使う予定」とある。工数増
- [ ] シェアボタンを実装（X / LINE / URLコピー / Web Share API）→ バックエンド不要、軽い
- [ ] dataLayerイベントを追加：
  - `reaction_like`（`item_id`, `item_type`, `liked`）
  - `reaction_share`（`share_method`, `item_id`）
- [ ] **`track.ts` の `Events` 型に必ず追記する**（表記ゆれ防止のため、計測は全部ここを通す設計になっている）
- [ ] GA4カスタムディメンション（テキスト）：`item_type`, `share_method`
- [ ] GA4カスタムメトリクス（数値）：メトリクスには**数値パラメータが必要**なので、イベント側に数値を持たせる設計にする

### ⑦ UU 30以上（自分を除く）

- [ ] 自分を除外する：GA4のデータフィルタ（内部トラフィック）＋ GTMでデバッグCookieや自分のIPを除外
- [ ] 30UUを集める算段を立てる（懸念2・懸念6）
- [ ] GA4の「ユーザー獲得」レポートでUUを確認

---

## 4. 懸念点

### 懸念1（大）このサイトはスクロールが起きない設計

DESIGN.mdに**決定事項として明記されている**：

> 「各ページとも情報量に対してページが長すぎる、PCで見たときスクロールしなくて済むくらいにまとめてほしい」という指示があった。これは「縦スクロールでのストーリーテリング」をやらない、という決定でもある

PCで開くと全部画面に収まる。するとスクロール深度は
- 25/50/75/100%が**開いた瞬間に全部同時に発火**する（画面内に収まっているため）か
- まったく発火しない

どちらでも「要件の形式は満たすが中身のない数字」になる。

**対策の選択肢**
- (a) モバイルは縦に伸びるので、**モバイルのデータを主として分析する**（`device_type`で切る）
- (b) スクロールが起きる長いページを1枚足す（**設計方針と衝突する**）
- (c) **「スクロールしない設計だったので深度指標が機能しなかった」を発見として報告する**

推奨は **(a) + (c)**。設計を曲げずに要件を満たし、かつ分析として正直。測定の目的は数字集めではなく設計判断の検証なので、これは価値のある結論になる。

### 懸念2（大）ゲートが計測の最大の障害

サイト本体に入るには
1. 44秒のオープニング（押せば飛ばせる）
2. **シューティングゲームで詐欺師8体撃退、または合言葉**

この2段構えが ③（3ページ以上遷移）と ⑦（UU30）の両方を殺しにくる。

- ゲートで諦めた人は他ページへ1回も行かない → ③のキーイベントに到達しない
- 初見だと「よく分からないサイト」で即離脱する率が高い

**対策**
- `gate_cleared` 変数を最初から入れる。**「何%がゲートで消えたか」が測れると、それ自体が最も面白い分析になる**
- 30UUを集める時、合言葉「こうきいつもありがとう」を先に伝える手もある（ただし本来の体験は歪む）
- ③が達成困難なら、提出時に**キーイベントの定義に「ゲート突破」も含める提案**をする

### 懸念3（中）いいね・シェア機能がまだ無い

⑥は「ユーザーリアクション行動を測れ」という要件だが、**測る対象の機能自体が存在しない**。実装から始まる。

- シェアは軽い（SNSリンク / Web Share API、バックエンド不要）
- いいねは方式次第。localStorageなら軽い、Supabaseなら総数が見えるが工数増
- **30UU規模では「いいね総数」の分析価値は低い**ので、localStorage方式で要件を満たすのが現実的

### 懸念4（大）GA4のデータしきい値で、30UUだとレポートが隠れる

GA4は、ユーザー数が少ないと個人が特定される恐れがあるとして**レポートのデータを自動的に隠す**（データのしきい値）。**30UUはまさにこれに引っかかる帯域。** 細かいディメンションで割ると「データがありません」になる。

**対策**
- **Googleシグナルをオフにする**（人口統計は諦める代わりに、しきい値の発生を抑えられる）
- レポートは粗い粒度で見る
- Looker Studioにも同じしきい値が効くので、細かく割りすぎない

### 懸念5（大）SPAなので、普通の設定では計測できない

`WarpLink` が `router.push()` を使う = **物理的なページロードが起きない**。

- GTMの「ページビュー」トリガーは**初回しか発火しない**
- GA4拡張計測の「履歴の変更に基づくページビュー」を**必ずON**にする
- GTMのスクロール深度トリガーはSPA遷移でリセットされない → History Changeで初期化が必要
- `document.referrer` は初回のみ有効 → sessionStorageで保持する

知らないと「なぜかデータが1ページ目だけ」という詰まり方をする。最初から織り込む。

### 懸念6（中）アドブロッカーとトラッキング防止

Safariのプライバシー保護やuBlock Origin等でGTM/GA4はブロックされる。実測で1〜3割が計測に乗らない。

**⑦の「UU30以上」を満たすには、実際には40〜50人に踏んでもらう必要がある**と見ておく。

### 懸念7（中）データ反映の遅れで作業順序が固定される

GA4の通常レポートは反映に**24〜48時間**かかる（リアルタイムレポートは即時）。

⑤のLooker Studioは**データが溜まってからしか作れない**。つまり順序が固定される：

```
実装 → 公開 → 人に見てもらう(数日) → GA4に溜まる(+1〜2日) → Looker作成
```

**スケジュールの逆算が必要。締切があるなら早めに共有してほしい。**

### 懸念8（小）プライバシー表記

公開サイトでGA4を使うので、Cookie利用とアクセス解析の告知が1行あると無難。2023年施行の電気通信事業法「外部送信規律」は個人サイトなら対象外の可能性が高いが、フッターに記載があると研修の評価としても印象が良い。

---

## 5. 進める順番（推奨）

1. **前提作業**（GA4/GTM作成、環境変数、拡張計測ON、Googleシグナルoff）← これが終わらないと何も検証できない
2. **②の`data-track`付与** — 既存の仕組みに乗るだけ。一番早く、効果も見える
3. **①③④⑥のコード側実装**（dataLayerに正しい形で流す）
4. **GTM/GA4の画面設定** — Claudeが手順書を書き、清水さんが押す
5. **公開 → 人に見てもらう**（⑦。ここに一番時間がかかる）
6. **⑥のいいね/シェア実装**は3と並行でも可
7. データが溜まったら **⑤のLooker Studio**

---

## 6. 判断が必要なこと（着手前に決めたい）

- [ ] **締切はいつか**（懸念7の逆算に必要）
- [ ] スクロール要件の扱い：懸念1の (a)+(c) で進めていいか
- [ ] いいねの方式：localStorage（軽い）か Supabase（総数が見える）か
- [ ] 30UUの集め方：誰に見てもらうか。合言葉を先に教えるか
- [ ] ③のキーイベント定義：「3ページ遷移」のままか、「ゲート突破」も加える提案をするか
